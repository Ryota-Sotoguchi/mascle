# 🏋️ Mascle - 筋トレ記録 & カロリー計算アプリ

筋トレメニューを記録し、MET値に基づいて消費カロリーを自動計算するフルスタックアプリケーション。

## アーキテクチャ

**Clean Architecture (DDD)** を採用し、依存関係は常に内側（Domain）に向きます。

```
┌─────────────────────────────────────┐
│  Presentation (Controllers/Routes)  │
├─────────────────────────────────────┤
│  Infrastructure (SQLite/DI)         │
├─────────────────────────────────────┤
│  Application (Use Cases/DTOs)       │
├─────────────────────────────────────┤
│  Domain (Entities/Value Objects)    │  ← 依存関係の方向
└─────────────────────────────────────┘
```

## 技術スタック

| レイヤー | 技術 |
|---------|------|
| Backend | Node.js, Express, TypeScript, SQLite (better-sqlite3) |
| Frontend | React 19, TypeScript, Vite, TailwindCSS |
| 構成 | npm Workspaces (Monorepo) |

## 機能

- ✅ ワークアウトセッションの作成・管理
- ✅ 30種類以上の筋トレメニュー（MET値付き）
- ✅ セット単位の記録（レップ数、重量、時間）
- ✅ MET値による消費カロリー自動計算
- ✅ 部位別フィルタリング（胸・背中・肩・腕・脚・体幹・全身・有酸素）
- ✅ ワークアウト履歴の表示

## セットアップ

### 前提条件

- **Node.js** v18以上
- **npm** v9以上

### インストールと起動

```bash
# 依存関係インストール
npm install

# 開発サーバー起動（Backend + Frontend同時）
npm run dev
```

- Backend API: http://localhost:3001/api
- Frontend: http://localhost:5173

### 個別起動

```bash
npm run dev:backend   # Backend のみ (port 3001)
npm run dev:frontend  # Frontend のみ (port 5173)
```

## API エンドポイント

| Method | Endpoint | 説明 |
|--------|----------|------|
| GET | `/api/exercises` | エクササイズ一覧 |
| GET | `/api/exercises?muscleGroup=chest` | 部位別フィルタ |
| POST | `/api/exercises` | エクササイズ作成 |
| GET | `/api/sessions` | セッション一覧 |
| GET | `/api/sessions/:id` | セッション詳細 |
| POST | `/api/sessions` | セッション作成 |
| POST | `/api/sessions/:id/sets` | セットを追加 |
| DELETE | `/api/sessions/:id` | セッション削除 |

## カロリー計算

MET (Metabolic Equivalent of Task) を使用:

$$\text{消費カロリー} = \text{MET} \times \text{体重(kg)} \times \text{時間(h)} \times 1.05$$

## プロジェクト構造

```
mascle/
├── packages/
│   ├── backend/
│   │   └── src/
│   │       ├── domain/          # エンティティ, 値オブジェクト, リポジトリIF
│   │       ├── application/     # ユースケース, DTO, マッパー
│   │       ├── infrastructure/  # SQLite実装, DI, シードデータ
│   │       └── presentation/    # Expressコントローラー, ルーティング
│   └── frontend/
│       └── src/
│           ├── api/             # APIクライアント
│           ├── components/      # Reactコンポーネント
│           ├── hooks/           # カスタムフック
│           └── types/           # 型定義
├── package.json                 # Monorepo設定
└── tsconfig.base.json           # TypeScript共通設定
```
